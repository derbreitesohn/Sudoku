using DataStructureLibrary.Graph;
namespace Sudoku.src.Models;

public class Board
{
    public void Space()
    {
        for (int r = 0; r < 9; r++) // r = row
        {
            for (int c = 0; c < 9; c++) // c = col
            {
                // 1. Einen neuen Vertex im Graphen erstellen

                // 2. Auf die 'Property' (unsere Cell) zugreifen

                // 3. Reihe und Spalte zuweisen
            }
        }
    }
}